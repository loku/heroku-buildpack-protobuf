#!/bin/sh

# TODO
# set PKG_CONFIG_PATH, PATH if VENDOR_PROTOBUF
#   need to resolve issues with multi release merging
#   even if multi merges, still no PATH merging
#     default buildpacks clobber PATH, LD_LIBRARY_PATH, etc

cat <<YAML
---
{}
YAML
